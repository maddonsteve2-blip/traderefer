export default function ReferrerProfileLayout({ children }: { children: React.ReactNode }) {
    return (
        <div className="pt-[72px] md:pt-[100px]">
            {children}
        </div>
    );
}

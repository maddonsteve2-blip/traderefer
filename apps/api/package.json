{
    "name": "api",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "python -m uvicorn main:app --host 0.0.0.0 --port 8000",
        "start": "python -m uvicorn main:app --host 0.0.0.0 --port 8000"
    }
}